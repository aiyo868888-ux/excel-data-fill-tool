<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 精确闹钟权限（Android 12+）-->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 开机启动权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- WiFi局域网同步权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ClipboardApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JishiClipboard"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JishiClipboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 编辑 Activity（透明对话框） -->
        <activity
            android:name=".ui.EditActivity"
            android:exported="false"
            android:theme="@style/Theme.Transparent"
            android:launchMode="singleTask"
            android:excludeFromRecents="true" />

        <!-- 类型选择 Activity（透明） -->
        <activity
            android:name=".ui.TypeSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.Transparent"
            android:launchMode="singleTop"
            android:excludeFromRecents="true" />

        <!-- 历史记录 Activity -->
        <activity
            android:name=".ui.HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.JishiClipboard" />

        <!-- 详情 Activity -->
        <activity
            android:name=".ui.DetailActivity"
            android:exported="false"
            android:theme="@style/Theme.JishiClipboard"
            android:parentActivityName=".ui.HistoryActivity" />

        <!-- 标签详情 Activity -->
        <activity
            android:name=".ui.TagDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.JishiClipboard" />

        <!-- 待办详情 Activity -->
        <activity
            android:name=".ui.TodoDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.JishiClipboard" />

        <!-- 标签管理 Activity -->
        <activity
            android:name=".ui.TagManageActivity"
            android:exported="false"
            android:theme="@style/Theme.JishiClipboard"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Web同步测试 Activity -->
        <activity
            android:name=".ui.WebSyncTestActivity"
            android:exported="true"
            android:theme="@style/Theme.JishiClipboard"
            android:label="Web同步测试" />

        <!-- 悬浮窗服务 -->
        <service
            android:name=".service.FloatingWindowService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_window" />
        </service>

        <!-- 剪贴板监听服务 -->
        <service
            android:name=".service.ClipboardMonitorService"
            android:enabled="true"
            android:exported="false" />

        <!-- Web同步服务 -->
        <service
            android:name=".service.WebSyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="web_sync" />
        </service>

        <!-- 提醒接收器 -->
        <receiver
            android:name=".reminder.ReminderReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.jishi.clipboard.ACTION_REMINDER" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
